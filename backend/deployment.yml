apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: tejaskalal/backend:70868778a69b7d145db10a577d9d3c8f8e61a910
          ports:
            - containerPort: 80
          env:
            - name: REDIS_HOST
              value: redis

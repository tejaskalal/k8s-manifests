apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: tejaskalal/backend:841e163d03707a99840065acf76c83e7f1287af8
          ports:
            - containerPort: 80
          env:
            - name: REDIS_HOST
              value: redis

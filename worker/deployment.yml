apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: tejaskalal/worker:latest
          env:
            - name: REDIS_HOST
              value: redis
